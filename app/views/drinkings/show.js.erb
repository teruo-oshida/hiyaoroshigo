$('#vote-modal').html('<%= escape_javascript(render 'show') %>');
$('#vote-modal').dialog({
  dialogClass: 'no-titlebar',
  draggable:   false,
  modal:       true,
  resizable:   false,
  width:       '84vw'
});
$('.close').click(function() {
  $('#vote-modal').dialog('close');
});
