# Use the README file as a reference to customize settings.

SakeMenuItem:
  fields:
    default: festival_id, restaurant_id, sake_id
    form: festival_id, restaurant_id, sake_id
  relationships: 
  application: Application
